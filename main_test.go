// main_test.go

package main

import "testing"

func TestValidMove(t *testing.T) {
	// Test cases for validMove function
	// Write tests to cover different scenarios and edge cases
	// Example:
	if !validMove(0, 0) {
		t.Errorf("Expected (0, 0) to be a valid move")
	}
}
